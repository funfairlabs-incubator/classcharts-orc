# Use .gitignore rules but explicitly re-include build-info.json
.gcloudignore
.git
.gitignore
node_modules
.next
vendor/shared/node_modules

# build-info.json is intentionally NOT excluded here
# It is generated by deploy-frontend.sh before gcloud app deploy runs
